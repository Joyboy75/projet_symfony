<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta
		name="viewport" content="width=device-width, initial-scale=1.0">
		<!-- Bootstrap !-->
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
		<link rel="stylesheet" href="{{asset('/css/style.css')}}">
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
		<link rel="stylesheet" href="{{asset('css/style.css')}}">
		<title>Document</title>
	</head>
	<body>
		<header>
			<nav class="navbar navbar-expand-lg navbar-dark bg-dark ">
				<div class="container-fluid ">
					<a class="navbar-brand" href="{{path('front_home')}}">Projet SF</a>
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>
					<div class="collapse navbar-collapse " id="navbarSupportedContent">
						<ul class="navbar-nav me-auto mb-2 mb-lg-0">
							<li class="nav-item">
								<a class="nav-link active" aria-current="page" href="{{path('product_list')}}">Products</a>
							</li>
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="{{ path("category_list")}}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
									Categories
								</a>
								<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
									<ul class="globals list">
										{% for grcategory in gcategories.getAll %}

											<li>
												<a href="{{path('category_show', {'id' : grcategory.id})}}">{{ grcategory.name }}</a>
											</li>

										{% endfor %}
									</ul>
								</ul>
							</li>
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="{{ path("licence_list")}}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
									Licences
								</a>

								<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
									<ul class="globals list">
										{% for grlicence in glicences.getAll %}

											<li>
												<a href="{{path('licence_show', {'id' : grlicence.id})}}">{{ grlicence.name }}</a>
											</li>

										{% endfor %}

									</ul>
								</ul>
							</li>


						</ul>

						
											<form class="d-flex" action="{{ path('front_search') }}" method="get">
												<input class="form-control me-2" type="search" name="term" placeholder="Rechercher" aria-label="Search">
												<button class="btn btn-outline-success" type="submit">Rechercher</button>
											</form>

											<div class="auth ms-auto p-2 bd-highlight " >
						{% if  app.user %}

							
								<a class="mb-1 me-1 " href="{{path('app_logout')}}" >Se déconnecter</a>
								<a class="mb-1 me-1 " href="{{path('app_login')}}" >Mon compte</a>

								

						{% endif %}

								<a href="{{path("show_cart")}}"><i class="bi bi-cart2" style="font-size: 2rem; color: cornflowerblue;"></i></a>
						{% if not app.user %}
							<a class="mb-1 me-1" href="{{path('app_login')}}" >Se connecter</a>
							<a class=" mb-1 me-1" href="{{path('user_insert')}}">S'inscrire</a>
						{% endif %}

					</div>
					</div>
					
				</div>
			</nav>


		</header>
		{% block main %}{% endblock %}

		{% block js %}{% endblock %}
	</body>

	
	<!-- Script Bootstrap !-->
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</html>
